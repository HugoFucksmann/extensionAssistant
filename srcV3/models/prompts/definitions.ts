// srcV3/models/prompts/definitions.ts

/**
 * Returns a map of all available prompt types and their definitions
 */
export function getPromptDefinitions(): Record<string, any> {
    return {
        // Add your prompt types here
        // Example:
        // 'codeGeneration': { description: 'Generate code based on requirements' },
        // 'codeReview': { description: 'Review code for issues' },
        // Add other prompt types as needed
    };
}